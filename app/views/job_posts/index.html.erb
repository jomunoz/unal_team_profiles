<div class="row clearfix">
  <div class="col-md-12 column">
    <h3>
      Anuncios de trabajo
    </h3>
    <table class="table table-striped table-condensed table-bordered">
      <thead>
        <tr>
          <th>ID</th>
          <th>Descripción</th>
          <th>Fuente</th>
          <th>Url</th>
        </tr>
      </thead>
      <tbody>
        <% @job_posts.each do |job_post| -%>
          <tr>
            <td><%= job_post.id %></td>
            <td><%= job_post.description %></td>
            <td><%= job_post.host %></td>
            <td><%= link_to job_post.source, job_post.source %></td>
            <td><%= link_to 'Ver', job_post_path(job_post) %></td>
          </tr>
        <% end -%>
      </tbody>
    </table> 
    <%= link_to 'Agregar anuncio', new_job_post_path, class: 'btn btn-primary' %>
  </div>
</div>
